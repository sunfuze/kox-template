module.exports = {
  application: {
    name: 'kox-template',
    description: 'kox template project',
    // keys for cookie sign
    keys: ['kox-template', 'secret'],
    // server port
    port: 3666,
    // support proxy
    proxy: true
  }
}
